apiVersion: v1
kind: Service
metadata:
  name: cybersec-dashboard
  namespace: cybersec-dashboard
spec:
  selector:
    app: cybersec-dashboard
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
