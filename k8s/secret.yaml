apiVersion: v1
kind: Secret
metadata:
  name: dashboard-secrets
  namespace: cybersec-dashboard
type: Opaque
# Set API_KEY manually — never commit a real key. Either:
#   kubectl -n cybersec-dashboard create secret generic dashboard-secrets \
#     --from-literal=API_KEY=<your-key> --dry-run=client -o yaml | kubectl apply -f -
# Or edit this file and: kubectl apply -f k8s/secret.yaml
stringData:
  API_KEY: ""