# OpenCode agent context: cybersec_dashboard_server

Communication default: caveman **lite**. Keep technical precision. Drop filler/hedging. Short sentences OK. Articles allowed. No pleasantries. Code blocks unchanged.

Switch caveman intensity with `/caveman lite|full|ultra`. Normal mode: `/normal` or `stop caveman`. Pretty words only when user explicitly commands.

Project: FastAPI cybersec news dashboard. Security state-changing endpoints require API_KEY + Authorization: Bearer. CORS default empty. TLS pending k3s cert-manager.
